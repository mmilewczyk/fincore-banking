spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:postgresql://localhost:5433/payment_db
    username: fincore
    password: fincore_secret
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      # Named pool for JMX/metrics identification
      pool-name: PaymentHikariCP
      # Leak detection: log if connection held > 60s (catches missing finally/try-with-resources)
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        # Batch inserts — OutboxPoller writes many rows; batching reduces DB round-trips
        jdbc.batch_size: 50
        order_inserts: true
        order_updates: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 10
      properties:
        # Exactly-once semantics at producer level (idempotent + transactional not needed here
        # because we use the Outbox pattern — Kafka is written by OutboxPoller, not in-request)
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5
        delivery.timeout.ms: 120000
    consumer:
      group-id: payment-service
      auto-offset-reset: earliest
      enable-auto-commit: false   # Manual ACK only — never lose a message
      max-poll-records: 50
      properties:
        # If consumer takes > 5min to process a batch, Kafka will rebalance
        max.poll.interval.ms: 300000

  data:
    redis:
      host: localhost
      port: 6379
      password: fincore_redis_secret

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/fincore
          jwk-set-uri: http://localhost:8180/realms/fincore/protocol/openid-connect/certs

server:
  port: 8082
  shutdown: graceful
  tomcat:
    # Tuned for K8s — payment processing is I/O bound (Account Service, FX Service, DB)
    threads:
      max: 50
      min-spare: 10
    connection-timeout: 5000

services:
  account:
    base-url: http://localhost:8081
  fx:
    base-url: http://localhost:8084

resilience4j:
  circuitbreaker:
    instances:
      account-service:
        registerHealthIndicator: true
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 15s
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: 3s
        eventConsumerBufferSize: 20
      fx-service:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        waitDurationInOpenState: 30s
        failureRateThreshold: 50
        eventConsumerBufferSize: 20
  retry:
    instances:
      account-service:
        maxAttempts: 3
        waitDuration: 500ms
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
        retryExceptions:
          - java.net.ConnectException
          - java.net.SocketTimeoutException
          - org.springframework.web.reactive.function.client.WebClientRequestException
        ignoreExceptions:
          # Don't retry business exceptions — they won't resolve on retry
          - java.lang.IllegalStateException
          - java.util.NoSuchElementException
      fx-service:
        maxAttempts: 2
        waitDuration: 1s
        # FX rates are time-sensitive — fewer retries, faster fail
  timelimiter:
    instances:
      account-service:
        timeoutDuration: 5s
      fx-service:
        timeoutDuration: 10s

outbox:
  poller:
    batch-size: 100       # Messages per poll cycle
    fixed-delay-ms: 500   # Interval between polls

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics,circuitbreakers,loggers,env
  endpoint:
    health:
      show-details: when-authorized
      probes:
        enabled: true        # /actuator/health/liveness + /actuator/health/readiness for K8s
  health:
    circuitbreakers:
      enabled: true
    redis:
      enabled: true
  metrics:
    tags:
      application: payment-service  # Common label on all metrics for Grafana filtering
  tracing:
    sampling:
      probability: 1.0    # 100% in dev — reduce to 0.1 in prod
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
  prometheus:
    metrics:
      export:
        enabled: true

logging:
  level:
    com.fincore: DEBUG
    org.springframework.kafka: INFO
    org.redisson: WARN   # Redisson is verbose; suppress unless debugging locks
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId}/%X{spanId}] %-5level %logger{36} - %msg%n"